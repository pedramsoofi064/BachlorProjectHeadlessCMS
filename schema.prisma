// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url      = env("DATABASE_URL")
  provider = "sqlite"
}

generator client {
  provider   = "prisma-client-js"
  output     = "node_modules/.prisma/client"
  engineType = "binary"
}

model User {
  id       String  @id @default(cuid())
  name     String  @default("")
  email    String  @unique @default("")
  isAdmin  Boolean @default(false)
  password String
}

model Post {
  id              String    @id @default(cuid())
  authorName_fa   String    @default("")
  authorName_en   String    @default("")
  title_fa        String    @default("")
  title_en        String    @default("")
  buttonTitle_fa  String    @default("test")
  buttonTitle_en  String    @default("test")
  summary_fa      String    @default("")
  summary_en      String    @default("")
  isActive        Boolean   @default(false)
  image_filesize  Int?
  image_extension String?
  image_width     Int?
  image_height    Int?
  image_mode      String?
  image_id        String?
  publishDate     DateTime?
  tags            Tag[]     @relation("Post_tags_Tag_posts")
  content_fa      String    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  content_en      String    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
}

model Tag {
  id      String @id @default(cuid())
  name_fa String @default("")
  name_en String @default("")
  posts   Post[] @relation("Post_tags_Tag_posts")
}

model Page {
  id             String        @id @default(cuid())
  pageURL        String        @default("")
  title          String        @default("")
  sections       PageSection[] @relation("PageSection_page")
  additionalData String?

  @@index([pageURL])
}

model PageSection {
  id               String  @id @default(cuid())
  sectionId        String  @default("")
  sectionTitle     String  @default("")
  backgroundImages Image[] @relation("PageSection_backgroundImages_many")
  contentTitle_fa  String  @default("")
  contentTitle_en  String  @default("")
  content_fa       String  @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  content_en       String  @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  isActive         Boolean @default(true)
  page             Page?   @relation("PageSection_page", fields: [pageId], references: [id])
  pageId           String? @map("page")
  additionalData   String?

  @@index([sectionId])
  @@index([pageId])
}

model Image {
  id                                String        @id @default(cuid())
  title                             String        @default("")
  alt                               String        @default("")
  image_filesize                    Int?
  image_extension                   String?
  image_width                       Int?
  image_height                      Int?
  image_mode                        String?
  image_id                          String?
  from_PageSection_backgroundImages PageSection[] @relation("PageSection_backgroundImages_many")
  from_Achievement_images           Achievement[] @relation("Achievement_images_many")
}

model NavigationMenu {
  id       String  @id @default(cuid())
  title_fa String  @default("")
  title_en String  @default("")
  pageURL  String  @default("")
  status   String? @default("enabled")
  type     String? @default("link")
}

model Partner {
  id             String  @id @default(cuid())
  name_fa        String  @default("")
  name_en        String  @default("")
  status         String? @default("enabled")
  logo_filesize  Int?
  logo_extension String?
  logo_width     Int?
  logo_height    Int?
  logo_mode      String?
  logo_id        String?
}

model Achievement {
  id             String  @id @default(cuid())
  slug           String  @default("")
  title_fa       String  @default("")
  title_en       String  @default("")
  buttonTitle_fa String  @default("")
  buttonTitle_en String  @default("")
  summary_fa     String  @default("")
  summary_en     String  @default("")
  images         Image[] @relation("Achievement_images_many")
  content_fa     String  @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  content_en     String  @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  isActive       Boolean @default(false)
}

model NewsletterSubmission {
  id   String @id @default(cuid())
  name String @default("")
}

model Footer {
  id             String  @id @default(cuid())
  description_fa String  @default("")
  description_en String  @default("")
  email          String  @default("")
  phone          String  @default("")
  address_fa     String  @default("")
  address_en     String  @default("")
  copyRigth_fa   String  @default("")
  copyRigth_en   String  @default("")
  status         String? @default("enabled")
}